[package]
name = "jarvis-spend-guard"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
jarvis-policy-interface = { path = "../policy-interface" }
